package hackerrank;

import java.util.Scanner;

public class Question11 {
	public static void main(String[] args) {
		Subclass subclass = new Subclass( new Outer() );
		// this subclass is a implementation of and inner class in the outer class but for that need to call in teh constructor and instance of the outer.
		System.out.print(subclass.value);
		Scanner scan = new Scanner(System.in);
		scan.hasNext()
	}
}
